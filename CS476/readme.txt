this is intended to be uploaded and run on the university's webdev sever.

all directories need to have execute/enter permissions.

the 'avatar', 'note', and 'photo' directories need to have write permissions, as the user will upload files to these directories.

